# Contributing

Use a focused branch and pull request. Run `composer check` and `node --check js/oncallforms.js`, add tests for behavior changes, and update the changelog. Never include production data, form contents, credentials, compiled dependencies, or invented screenshots. Changes to DOM selectors must be checked against the current GLPI 11 templates and documented in `docs/architecture.md`.
